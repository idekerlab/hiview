import {createMuiTheme} from 'material-ui/styles';
import {teal, pink, red} from 'material-ui/colors'

export const theme = createMuiTheme({
  palette: {
    primary: teal,
    accent: pink,
    error: red,
  },
});
